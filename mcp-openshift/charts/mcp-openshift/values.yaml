namespace:
  create: false
  name: mcp-server

replicaCount: 1

image:
  repository: quay.io/fcalomen/mcp
  tag: "openshift-0.0.11"
  pullPolicy: IfNotPresent

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  name: ""

auth:
  enabled: true
  existingSecret: ""
  secretName: ""
  tokenKey: token
  token: ""

mcp:
  allowedHosts:
    # Add your allowed hostnames here (e.g., "mcp.example.com", "*.apps.cluster.openshift.com:*")
    - 127.0.0.1:*
    - localhost:*
  allowedOrigins:
    # Add your allowed origins here (e.g., "https://console.example.com", "https://*.apps.cluster.openshift.com:*")
    - http://127.0.0.1:*
    - http://localhost:*

rbac:
  create: true

service:
  type: ClusterIP
  port: 8000

route:
  enabled: true
  host: ""
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 1001
  seccompProfile:
    type: RuntimeDefault

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop:
      - ALL

nodeSelector: {}
tolerations: []
affinity: {}
